import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "@/components/auth";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BrandPilot AI | AI Visibility Auditor",
  description: "Discover how ChatGPT, Gemini, and Perplexity perceive your brand. Get actionable insights to improve your AI visibility with BrandPilot AI.",
  keywords: ["BrandPilot", "Generative Engine Optimization", "AI visibility", "ChatGPT", "Gemini", "Perplexity", "brand reputation"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <AuthProvider>
          {children}
        </AuthProvider>
      </body>
    </html>
  );
}
